This is the TagWW Mistika Repository

## How to install this repository

When cloning this repository, ensure you rename to `SGO AppData` and place it at the root of the active user. For example

```
git clone https://github.com/edkonsistent/tagww-mistika.git '/Users/[username]/SGO AppData'
```
